language: rust
sudo: 9000
dist: trusty
rust:
  - nightly
os:
  - linux
  - osx
env:
  - FEATURES=""
  - FEATURES="--features debugmozjs"

branches:
  except:
    - master
notifications:
  webhooks: http://build.servo.org:54856/travis

script: |
  set -e
  export RUST_TEST_THREADS=1
  travis_wait cargo build --verbose $FEATURES
  cargo test --verbose $FEATURES
  cargo run --example callback $FEATURES
